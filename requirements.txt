# Sampler Editor - Requirements
# GUI Framework
PySide6>=6.6.0

# Audio processing libraries
numpy>=1.24.0
soundfile>=0.12.1
sounddevice>=0.4.6
scipy>=1.10.0

# Pitch detection
crepe>=0.0.13
tensorflow>=2.13.0

# Backup pitch detection (optional)
aubio>=0.4.9

# Audio analysis
librosa>=0.10.1

# File handling and utilities
pathlib2>=2.3.7; python_version<"3.4"

# Development and debugging
pytest>=7.4.0
pytest-qt>=4.2.0

# Optional: For advanced audio features
# resampy>=0.4.2
# numba>=0.57.0

# Note: TensorFlow comes with CREPE but might need specific versions
# For CPU-only (lighter): tensorflow-cpu>=2.13.0
# For GPU support: tensorflow-gpu>=2.13.0 (if you have CUDA setup)

# Installation notes:
# 1. Install with: pip install -r requirements.txt
# 2. For development: pip install -r requirements.txt -e .
# 3. If you encounter TensorFlow issues, try:
#    pip install tensorflow==2.13.0 --no-deps
#    pip install crepe --no-deps
# 4. For Apple Silicon Macs, use: pip install tensorflow-macos tensorflow-metal